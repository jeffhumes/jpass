[package]
name = "jpass-android"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }

jpass-core = { path = "../jpass-core" }
jpass-platform = { path = "../jpass-platform" }
