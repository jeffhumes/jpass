[package]
name = "jpass-desktop"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
rusqlite = { version = "0.32", features = ["bundled"] }
directories = "5"
arboard = "3"

jpass-core = { path = "../jpass-core" }
jpass-platform = { path = "../jpass-platform" }
